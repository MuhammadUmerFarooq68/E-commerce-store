version: '3.1'

services:
  api:
    build:
      context: 
    ports:
      - "3000:3000" 
    environment:
      - DB_HOST= # Change to the service name, not localhost
      - DB_USER=
      - DB_PASS=
      - DB_NAME=
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD:
      MYSQL_DATABASE: 
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
